{
  "name": "partyparty-site",
  "private": true,
  "version": "0.0.0",
  "description": "partyparty.party — landing page, account/event APIs, broker, and app downloads served by one Worker.",
  "type": "module",
  "scripts": {
    "check": "node --check worker.js",
    "smoke": "node test/smoke.mjs",
    "login": "wrangler login",
    "whoami": "wrangler whoami",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "wrangler": "^4.106.0"
  }
}
